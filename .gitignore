# Environment
.env
.venv/
.venv-1/

# Python
__pycache__/
*.pyc
*.egg-info/

# Training checkpoints and intermediate results
results/

# Trained model weights and tokenizer (too large — regenerate by training)
artifacts/*/model/
artifacts/*/tokenizer/
# Stray root-level model/tokenizer from old runs
artifacts/model/
artifacts/tokenizer/

# Pip temp dirs that accidentally ended up in artifacts/
artifacts/pip-*/

# Intermediate experiment artifacts — only final three variants kept
artifacts/image/
artifacts/image_clip_ocr/
artifacts/multimodal/
artifacts/multimodal_clip_ocr/
artifacts/metadata.json
artifacts/token_length_report.json

# Old policy/overview report files superseded by snapshot
artifacts/reports/block_precision_by_age.png
artifacts/reports/block_recall_by_age.png
artifacts/reports/protection_rate_by_age.png
artifacts/reports/policy_metrics_by_age.csv
artifacts/reports/overview.csv
artifacts/reports/roc_auc_by_label.csv
artifacts/reports/summary_report.md

# Backup directories
artifacts_backup_*/

# Image feature vectors (.npy files — regenerate with extract_image_features.py)
data/image_features/
data/image_features_*/

# Training data and thumbnails
data/*.xlsx
data/thumbnails/
